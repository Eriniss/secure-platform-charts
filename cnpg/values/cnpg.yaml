image:
  tag: "1.30.1"

replicaCount: 1

config:
  clusterWide: true

monitoring:
  podMonitorEnabled: false
  grafanaDashboard:
    create: false

nodeSelector:
  openebs.io/lvm-ready: "true"

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    memory: 256Mi
