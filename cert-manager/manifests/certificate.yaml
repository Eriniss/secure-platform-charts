apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: polipixel-tls
  namespace: gateway
spec:
  secretName: polipixel-tls
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
  dnsNames:
    - "polipixel.link"
    - "*.polipixel.link"
