apiVersion: v1
kind: Pod
metadata:
  name: toolbox
  namespace: default
spec:
  containers:
  - name: toolbox
    image: nicolaka/netshoot:latest
    command: ["sleep", "infinity"]
  restartPolicy: Always